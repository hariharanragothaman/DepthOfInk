# DepthOfInk Backend - MVP
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
pydantic>=2.5.0
pydantic-settings>=2.1.0

# PDF: PyMuPDF first, pdfplumber fallback for layout-heavy PDFs
pymupdf>=1.23.0
pdfplumber>=0.10.0

# Embeddings + LLM
openai>=1.12.0

# AWS Bedrock (optional: only needed if LLM_PROVIDER=bedrock)
boto3>=1.34.0

# Vector store
chromadb>=0.4.22

# Utilities
tiktoken>=0.5.0
httpx>=0.26.0
